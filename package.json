{
  "name": "finefoods",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && bun tests/verify-built-cms-assets.ts && bun tests/verify-built-living-design.ts && bun tests/verify-built-catalog.ts && bun tests/verify-built-brands.ts && bun tests/verify-built-enquiry.ts && bun tests/verify-built-mvp.ts && bun tests/verify-built-route-manifest.ts",
    "check": "astro check",
    "test": "bun test",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types"
  },
  "dependencies": {
    "@fontsource/newsreader": "^5.2.10",
    "@fontsource/nunito": "^5.2.7",
    "@iconify-json/lucide": "^1.2.117",
    "astro": "^7.0.9",
    "astro-icon": "^1.1.5",
    "wrangler": "^4.112.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.0"
  }
}
