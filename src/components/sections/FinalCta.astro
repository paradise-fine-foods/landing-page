---
import ButtonLink from '../global/ButtonLink.astro';
interface Props { title: string; description: string; button: string; label: string; href: string }
const { title, description, button, label, href } = Astro.props;
---

<section class="final-cta" aria-labelledby="final-cta-title">
  <div class="container final-cta__grid">
    <p>{label}</p>
    <div><h2 id="final-cta-title">{title}</h2><p>{description}</p></div>
    <ButtonLink {href}>{button}</ButtonLink>
  </div>
</section>

<style>
  .final-cta { background: var(--color-cold-chain-blue); color: var(--color-paper-white); padding-block: var(--space-7); position: relative; }
  .final-cta::before { background: var(--color-cultured-butter); block-size: 0.35rem; content: ''; inline-size: clamp(5rem, 14vw, 12rem); inset-block-start: 0; inset-inline-start: var(--container-inline); position: absolute; }
  .final-cta__grid { align-items: center; display: grid; gap: var(--space-6); grid-template-columns: 0.7fr 2fr auto; }
  .final-cta__grid > p { font-size: var(--text-xs); letter-spacing: var(--tracking-label); }
  .final-cta h2 { font-size: clamp(2rem, 4vw, 3.8rem); }
  .final-cta h2 + p { margin-block-start: var(--space-2); max-inline-size: 42rem; }
  .final-cta :global(.button-link--primary) { background: var(--color-paper-white); color: var(--color-carbon); }
  @media (max-width: 48rem) { .final-cta__grid { grid-template-columns: 1fr; } .final-cta__grid > p { display: none; } }
</style>
