---
interface Props {
  href: string;
  variant?: 'primary' | 'secondary';
  class?: string;
}

import Icon from '@/components/global/Icon.astro';

const { href, variant = 'primary', class: className } = Astro.props;
---

<a class:list={['button-link', `button-link--${variant}`, className]} href={href}>
  <slot />
  <Icon name="lucide:arrow-up-right" size="1.05em" />
</a>

<style>
  .button-link {
    align-items: center;
    border: 1px solid var(--color-deep-herb);
    border-radius: var(--radius-sm);
    display: inline-flex;
    font-size: var(--text-sm);
    font-weight: 600;
    gap: var(--space-3);
    justify-content: space-between;
    min-block-size: 2.75rem;
    padding: var(--space-3) var(--space-5);
    text-decoration: none;
    transition: background-color var(--transition-fast), border-color var(--transition-fast), color var(--transition-fast), box-shadow var(--transition-fast);
  }

  .button-link--primary {
    background: var(--color-deep-herb);
    border-color: var(--color-deep-herb);
    color: var(--color-process-white);
  }

  .button-link--primary:hover {
    background: var(--color-deep-herb);
    border-color: var(--color-paradise-orange);
    color: var(--color-process-white);
    box-shadow: 0 0 0 2px var(--color-paradise-orange);
  }

  .button-link--secondary {
    background: var(--color-glass-surface);
    border-color: var(--color-deep-herb);
    color: var(--color-deep-herb);
    -webkit-backdrop-filter: blur(var(--blur-glass));
    backdrop-filter: blur(var(--blur-glass));
  }

  .button-link--secondary:hover {
    background: var(--color-process-white);
    border-color: var(--color-paradise-orange);
  }

  .button-link :global(svg) { color: var(--color-paradise-orange); }
</style>
